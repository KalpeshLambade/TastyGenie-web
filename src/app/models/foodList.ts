export interface Recipe {
    estimateTime: string;
    imageUrl: string;
    cuisine: string;
}

export interface formData {
  ingredients: string;
  appliances: string;
  preferences: string;
  cuisine: string;
}